describe("The model", function() {
		var mocksummaries = require('./mocks/resumeSummaries').mock;
		var messenger = require('messenger');
		var resumeModel = require('app/resume/resumeModel');
		var MongoClient = require('mongodb').MongoClient();

		beforeEach(function() {
		});
		afterEach(function() {
		});

		it("returns resume summary for a user", function(done) {
		});
		it('returns skills for a user', function(done) {
		});
		it('returns companies for a user', function(done) {
		});
		it('returns the years of experience for a user', function(done) {
		});
});
